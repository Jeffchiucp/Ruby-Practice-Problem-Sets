# Q4 What will each of the following method calls do?
# (assume we reset numbers to original between calls)

numbers = [1, 2, 3, 4, 5]

numbers.delete_at(1) # => numbers is now [1, 3, 4, 5]
numbers.delete(1) # => numbers is now [2, 3, 4, 5]
